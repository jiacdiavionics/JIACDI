name: CI Pull Request

on:
  pull_request:
    branches: [master, main, develop]
  workflow_dispatch:

jobs:
  build-release:
    name: Build Release
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 10

      - name: Build
        shell: pwsh
        run: |
          Write-Host "=== Environment Check ==="
          Write-Host "DOTNET_VERSION: $env:DOTNET_VERSION"
          $msbuildPath = Split-Path (& "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" -latest -requires Microsoft.Component.MSBuild -find MSBuild\Current\Bin\amd64\MSBuild.exe | Select-Object -First 1) -Parent
          Write-Host "MSBuild Path: $msbuildPath"
          $env:PATH = $msbuildPath + ';' + $env:PATH
          nuget locals all -clear
          Write-Host "=== Starting Build ==="
          msbuild -v:m -restore -t:Build -p:Configuration=Release MissionPlanner.sln 2>&1 | tee build.log
          Write-Host "=== Build Output ==="
          Select-String -Path build.log -Pattern "error|warning|Build FAILED|Build succeeded" | Select-Object -First 20

      - uses: actions/upload-artifact@v4
        with:
          name: DIMP-Release
          path: bin/Release/net461

  build-debug:
    name: Build Debug
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 10

      - name: Cache multiple paths
        uses: actions/cache@v4
        with:
          path: |
            **/obj/**
          key: ${{ runner.os }}-buildDebug

      - name: Build
        shell: pwsh
        run: |
          $msbuildPath = Split-Path (& "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" -latest -requires Microsoft.Component.MSBuild -find MSBuild\Current\Bin\amd64\MSBuild.exe | Select-Object -First 1) -Parent
          $env:PATH = $msbuildPath + ';' + $env:PATH
          nuget locals all -clear
          msbuild -v:m -restore -t:Build -p:Configuration=Debug MissionPlanner.sln

      - uses: actions/upload-artifact@v4
        with:
          name: DIMP-Debug
          path: bin/Debug/net461
